version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/var/www/html
    working_dir: /var/www/html
    command: ["php-fpm"]
    expose:
      - "9000"

  nginx:
    image: nginx:alpine
    container_name: nginx
    ports:
      - "80:80"  # Cambiado a 80 para que coincida con la configuración de Nginx
    volumes:
      - .:/var/www/html
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - web

  mongodb:
    image: mongo:latest
    ports:
      - "27017:27017"

  redis:
    image: redis:alpine
    container_name: redis

volumes:
  db_data:
